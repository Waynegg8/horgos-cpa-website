---
pagination:
  data: calculators
  size: 1
  alias: calculator
permalink: /calculators/{{ calculator.id | slug }}/
layout: layouts/page.njk
title: "{{ calculator.title }}"
description: "計算機：{{ calculator.description }}"
---

<div class="calculator-container">
  <h1 class="text-3xl font-bold mb-4">{{ calculator.title }}</h1>
  <p class="mb-6 text-gray-600">{{ calculator.description }}</p>

  {# 這會將您在 calculators.json 中定義的 HTML 和 JS 程式碼插入到這裡 #}
  <div class="calculator-content">
    {{ calculator.content | safe }}
  </div>

  <div class="mt-8">
    {# 注意：這個連結是指向總覽頁面 /calculators/，而不是 /calculators.md #}
    <a href="/calculators/" class="text-blue-600 hover:underline">&larr; 返回常用工具列表</a>
  </div>
</div>