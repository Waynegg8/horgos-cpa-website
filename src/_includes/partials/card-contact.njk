{# src/_includes/partials/card-contact.njk #}
{#
  聯絡資訊卡片佈局。
  接收一個 `contact` 變數 (通常是 src/_data/contact.json)。
  符合藍圖的卡片視覺設計，顯示地址、電話、Line 按鈕、營業時間。
#}
<div class="bg-white p-6 rounded-lg shadow-lg flex flex-col h-full">
  <h3 class="text-2xl font-heading font-bold text-brand-blue-700 mb-4">聯絡資訊</h3>
  <ul class="space-y-4 text-gray-700 text-lg flex-grow">
    <li class="flex items-start">
      <span class="material-icons text-brand-blue-600 mr-3 text-2xl mt-1">location_on</span>
      <span>{{ contact.address }}</span>
    </li>
    <li class="flex items-center">
      <span class="material-icons text-brand-blue-600 mr-3 text-2xl">phone</span>
      <a href="tel:{{ contact.phone | replace(' ', '') }}" class="hover:underline">{{ contact.phone }}</a>
    </li>
    <li class="flex items-center">
      <span class="material-icons text-brand-blue-600 mr-3 text-2xl">access_time</span>
      <div class="flex flex-col">
        {% for hour in contact.businessHours %}
          <span>{{ hour.day }} {{ hour.time }}</span>
        {% endfor %}
      </div>
    </li>
  </ul>
  <div class="mt-6 flex-shrink-0"> {# 將按鈕推到底部 #}
    <a href="{{ contact.lineUrl }}" target="_blank" rel="noopener noreferrer" class="inline-flex items-center bg-[#00C300] text-white font-bold py-3 px-6 rounded-lg hover:bg-green-600 transition duration-200 shadow-md">
      <img src="https://simpleicons.org/icons/line.svg" alt="Line" class="w-6 h-6 mr-2">
      加入 Line 好友
    </a>
  </div>
</div>