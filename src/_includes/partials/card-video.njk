{# src/_includes/partials/card-video.njk #}
{#
  影片卡片佈局。
  接收一個 `item` 變數，它應該是 Eleventy 影片集合中的一個影片物件。
  將影片縮圖與影片標題、日期、摘要連結起來。
  符合藍圖的卡片視覺設計。
#}
<div class="bg-white rounded-lg shadow-md hover:shadow-lg transition-shadow duration-200 overflow-hidden flex flex-col h-full">
  {% if item.thumbnailUrl %}
    <a href="/video/{{ item.slug }}/" class="block flex-shrink-0 relative aspect-video">
      {# 影片縮圖通常直接引用 URL，但如果需要優化本地影片縮圖，可以考慮 Eleventy Image shortcode #}
      <img src="{{ item.thumbnailUrl }}" alt="{{ item.title }} 影片縮圖" class="w-full h-full object-cover">
      <div class="absolute inset-0 flex items-center justify-center bg-black bg-opacity-40 text-white opacity-0 hover:opacity-100 transition-opacity duration-200">
        <span class="material-icons text-6xl">play_circle_filled</span>
      </div>
    </a>
  {% endif %}
  <div class="p-6 flex flex-col flex-grow">
    <h3 class="text-xl font-heading font-bold text-brand-blue-700 mb-2 flex-shrink-0">
      <a href="/video/{{ item.slug }}/" class="hover:underline">{{ item.title }}</a>
    </h3>
    <p class="text-gray-600 text-sm mb-3 flex-shrink-0">
      <time datetime="{{ item.date }}">{{ item.date | formatDate }}</time>
    </p>
    <p class="text-gray-700 text-base leading-relaxed line-clamp-3 mb-4 flex-grow">
      {{ item.excerpt }}
    </p>
    <div class="mt-auto flex-shrink-0"> {# 將 CTA 按鈕推到底部 #}
      <a href="/video/{{ item.slug }}/" class="inline-flex items-center text-brand-blue-600 hover:text-brand-blue-800 font-semibold transition-colors duration-200">
        觀看影片
        <span class="material-icons ml-1 text-lg">arrow_forward</span>
      </a>
    </div>
  </div>
</div>