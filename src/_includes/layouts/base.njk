<!DOCTYPE html>
<html lang="zh-Hant-TW">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    
    {# 頁面標題會動態載入，並在後面加上網站名稱 #}
    <title>{{ title }} | {{ site_meta.name }}</title>
    
    {# 網站描述，優先使用頁面本身的 description，若無則使用全站預設值 #}
    <meta name="description" content="{{ description or site_meta.description }}">
    
    {# 引入編譯後的 Tailwind CSS 檔案 #}
    <link rel="stylesheet" href="{{ '/assets/css/styles.css' | url }}">
    
    {# 未來可用於放置網站圖示、GA分析碼等 #}
    <link rel="icon" href="/favicon.ico">

</head>
<body class="bg-white text-gray-800 font-sans">

    {# 網站頂部導航列 #}
    <header class="bg-white shadow-md sticky top-0 z-50">
      {% include "partials/header.njk" %}
    </header>

    {# 主要內容區塊 #}
    <main>
        {# 所有繼承此版型的頁面內容，都會被插入到這裡 #}
        {{ content | safe }}
    </main>

    {# === [任務 3.3 新增] === #}
    {# 在頁尾正上方，引入 SEO 內容區塊 #}
    {% include "partials/seo-footer-block.njk" %}

    {# 網站頁尾 #}
    <footer class="bg-gray-800 text-white p-8">
      {% include "partials/footer.njk" %}
    </footer>

    {# 引入主要的 JavaScript 檔案 #}
    <script src="{{ '/assets/js/main.js' | url }}"></script>

</body>
</html>