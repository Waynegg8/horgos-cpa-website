{% extends "layouts/base.njk" %}

{% block content %}
<article class="prose lg:prose-xl mx-auto px-4 py-8">
  <h1>{{ title }}</h1>
  <p>我們提供以下文件與資源供您下載使用。</p>

  <div class="space-y-6">
    {# 讀取由 Python 腳本生成的 processed_downloads.json #}
    {% for item in processed_downloads %}
    <div class="p-4 border rounded-lg">
        <h3 class="font-bold text-lg">{{ item.title }}</h3>
        <p class="text-gray-600">{{ item.description }}</p>
        <a href="{{ item.path | url }}" class="inline-block bg-primary text-white font-bold py-2 px-4 rounded mt-2" download>下載檔案</a>
    </div>
    {% else %}
    <p>目前沒有可下載的資源。</p>
    {% endfor %}
  </div>

</article>
{% endblock %}