{# src/_includes/components/process-steps.njk #}
{#
  步驟/流程圖組件。
  接收 `data` 變數，它是一個包含步驟物件的陣列。
  每個步驟包含 `step_number` (數字), `title` (字串), `description` (字串)。
  符合藍圖的組件視覺設計。
#}
{% if data and data.length %}
<div class="py-8 md:py-12 bg-white"> {# 可以根據需要調整背景色 #}
  <div class="container mx-auto px-4">
    {% if title %}
      <h2 class="text-3xl md:text-4xl font-heading font-bold text-center text-brand-blue-700 mb-10">{{ title }}</h2>
    {% endif %}

    <div class="flex flex-col lg:flex-row lg:justify-center lg:items-start gap-8">
      {% for step in data %}
      <div class="flex flex-col items-center lg:items-start lg:w-1/3 text-center lg:text-left relative">
        {# 步驟編號圓圈 #}
        <div class="w-12 h-12 rounded-full bg-brand-blue-700 text-white flex items-center justify-center text-xl font-bold mb-4 flex-shrink-0">
          {{ step.step_number }}
        </div>
        {# 連接線 (僅適用於水平佈局時) #}
        {% if not loop.last and data.length > 1 %}
          <div class="hidden lg:block absolute left-1/2 top-6 h-1 w-[calc(100%+2rem)] -translate-x-1/2 bg-gray-300"></div>
          {# 箭頭 (可選) #}
          <div class="hidden lg:block absolute left-[calc(50%+100%-2rem)] top-5 transform -translate-x-1/2 text-gray-300">
            <span class="material-icons">arrow_right_alt</span> {# 使用 Material Icon 箭頭 #}
          </div>
        {% endif %}
        
        <h3 class="text-2xl font-heading font-bold text-brand-blue-700 mb-2">{{ step.title }}</h3>
        <p class="text-gray-700 text-base leading-relaxed">{{ step.description }}</p>
      </div>
      {% endfor %}
    </div>
  </div>
</div>
{% endif %}