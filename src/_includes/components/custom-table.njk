{# src/_includes/components/custom-table.njk #}
{#
  複雜表格組件。
  接收 `data` 物件，包含 `caption` (字串), `headers` (字串陣列), `rows` (陣列，每個元素為字串陣列)。
  支援響應式水平捲動。
  符合藍圖的表格視覺設計。
#}
{% if data %}
<div class="overflow-x-auto bg-white rounded-lg shadow-md mb-8">
  <table class="min-w-full divide-y divide-gray-200">
    {% if data.caption %}
      <caption class="p-5 text-left text-xl font-heading font-bold text-brand-blue-700 caption-top">{{ data.caption }}</caption>
    {% endif %}
    <thead class="bg-gray-50">
      <tr>
        {% for header in data.headers %}
          <th scope="col" class="px-6 py-3 text-left text-sm font-heading font-semibold text-gray-700 uppercase tracking-wider">
            {{ header }}
          </th>
        {% endfor %}
      </tr>
    </thead>
    <tbody class="bg-white divide-y divide-gray-200">
      {% for row in data.rows %}
        <tr>
          {% for cell in row %}
            <td class="px-6 py-4 whitespace-nowrap text-base text-gray-700">
              {{ cell | safe }} {# 使用 safe 過濾器以允許 HTML 內容或 Markdown 轉換 #}
            </td>
          {% endfor %}
        </tr>
      {% endfor %}
    </tbody>
    {# 您可以根據需要添加 tfoot 區塊 #}
  </table>
</div>
{% endif %}