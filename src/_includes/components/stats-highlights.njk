{# src/_includes/components/stats-highlights.njk #}
{#
  數據亮點/里程碑區塊組件。
  接收 `data` 物件，包含 `title`, `description`, `background_color`, `items` (物件陣列，每個包含 `value` 和 `description`)。
  符合藍圖的組件視覺設計。
#}
{% if data and data.items and data.items.length %}
<section class="py-12 md:py-16 {{ data.background_color or 'bg-white' }}">
  <div class="container mx-auto px-4">
    {% if data.title %}
      <h2 class="text-3xl md:text-4xl font-heading font-bold text-center text-brand-blue-700 mb-6">{{ data.title }}</h2>
    {% endif %}
    {% if data.description %}
      <p class="text-center text-gray-700 text-lg leading-relaxed max-w-3xl mx-auto mb-10">{{ data.description }}</p>
    {% endif %}

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-{{ data.items.length > 3 ? '4' : data.items.length }} gap-8 text-center">
      {% for item in data.items %}
      <div class="p-6 rounded-lg bg-white shadow-md"> {# 可選：可以將這裡的背景色設為透明或其他樣式 #}
        <p class="text-4xl md:text-5xl font-heading font-extrabold text-brand-blue-800 mb-2">{{ item.value }}</p> {# 數字/文字亮點本身字體可放大 #}
        <p class="text-gray-700 text-base leading-relaxed">{{ item.description }}</p> {# 描述內容統一應用內文樣式 #}
      </div>
      {% endfor %}
    </div>
  </div>
</section>
{% endif %}