
{# 
  可重用的卡片元件 - 樣式已從舊網站遷移並轉換為 Tailwind CSS
  - 增加了 'animate-on-scroll' class 來觸發動畫
#}
<a href="{{ url }}" class="
  block 
  bg-white 
  rounded-lg 
  shadow-md 
  hover:shadow-xl 
  transition-all 
  duration-300 
  transform 
  hover:-translate-y-1 
  overflow-hidden
  animate-on-scroll
">
  {% if image %}
    <img src="{{ image }}" alt="{{ title }}" class="w-full h-48 object-cover">
  {% endif %}
  <div class="p-6">
    <h3 class="text-xl font-bold text-gray-800 mb-2">{{ title }}</h3>
    <p class="text-gray-600">{{ description }}</p>
  </div>
</a>
