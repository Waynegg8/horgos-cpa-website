{
  "name": "hugo-accounting",
  "version": "1.0.0",
  "description": "Hugo會計事務所網站",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --minify",
    "test:performance": "node scripts/performance-test.js",
    "test:accessibility": "node scripts/accessibility-test.js",
    "test:final": "node scripts/final-check.js",
    "test": "npm run test:performance && npm run test:accessibility && npm run test:final",
    "deploy": "wrangler publish",
    "clean": "rm -rf public resources"
  },
  "dependencies": {
    "@axe-core/puppeteer": "^4.8.1",
    "chalk": "^4.1.2",
    "glob": "^10.3.10",
    "puppeteer": "^21.6.1"
  },
  "devDependencies": {
    "lighthouse": "^11.4.0",
    "wrangler": "^3.22.1"
  }
}